package discord.gamesdk;// Generated by jextract

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.util.function.*;

import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * struct IDiscordImageManager {
 *     void (*fetch)(struct IDiscordImageManager *, struct DiscordImageHandle, bool, void *, void (*)(void *, enum EDiscordResult, struct DiscordImageHandle));
 *     enum EDiscordResult (*get_dimensions)(struct IDiscordImageManager *, struct DiscordImageHandle, struct DiscordImageDimensions *);
 *     enum EDiscordResult (*get_data)(struct IDiscordImageManager *, struct DiscordImageHandle, uint8_t *, uint32_t);
 * }
 * }
 */
public class IDiscordImageManager {

    IDiscordImageManager() {
        // Should not be called directly
    }

    private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
        discord_game_sdk_h.C_POINTER.withName("fetch"),
        discord_game_sdk_h.C_POINTER.withName("get_dimensions"),
        discord_game_sdk_h.C_POINTER.withName("get_data")
    ).withName("IDiscordImageManager");

    /**
     * The layout of this struct
     */
    public static final GroupLayout layout() {
        return $LAYOUT;
    }

    /**
     * {@snippet lang=c :
     * void (*fetch)(struct IDiscordImageManager *, struct DiscordImageHandle, bool, void *, void (*)(void *, enum EDiscordResult, struct DiscordImageHandle))
     * }
     */
    public static class fetch {

        fetch() {
            // Should not be called directly
        }

        /**
         * The function pointer signature, expressed as a functional interface
         */
        public interface Function {
            void apply(MemorySegment _x0, MemorySegment _x1, boolean _x2, MemorySegment _x3, MemorySegment _x4);
        }

        private static final FunctionDescriptor $DESC = FunctionDescriptor.ofVoid(
            discord_game_sdk_h.C_POINTER,
            DiscordImageHandle.layout(),
            discord_game_sdk_h.C_BOOL,
            discord_game_sdk_h.C_POINTER,
            discord_game_sdk_h.C_POINTER
        );

        /**
         * The descriptor of this function pointer
         */
        public static FunctionDescriptor descriptor() {
            return $DESC;
        }

        private static final MethodHandle UP$MH = discord_game_sdk_h.upcallHandle(Function.class, "apply", $DESC);

        /**
         * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
         * The lifetime of the returned segment is managed by {@code arena}
         */
        public static MemorySegment allocate(Function fi, Arena arena) {
            return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
        }

        private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

        /**
         * Invoke the upcall stub {@code funcPtr}, with given parameters
         */
        public static void invoke(MemorySegment funcPtr,MemorySegment _x0, MemorySegment _x1, boolean _x2, MemorySegment _x3, MemorySegment _x4) {
            try {
                 DOWN$MH.invokeExact(funcPtr, _x0, _x1, _x2, _x3, _x4);
            } catch (Throwable ex$) {
                throw new AssertionError("should not reach here", ex$);
            }
        }
    }

    private static final AddressLayout fetch$LAYOUT = (AddressLayout)$LAYOUT.select(groupElement("fetch"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * void (*fetch)(struct IDiscordImageManager *, struct DiscordImageHandle, bool, void *, void (*)(void *, enum EDiscordResult, struct DiscordImageHandle))
     * }
     */
    public static final AddressLayout fetch$layout() {
        return fetch$LAYOUT;
    }

    private static final long fetch$OFFSET = 0;

    /**
     * Offset for field:
     * {@snippet lang=c :
     * void (*fetch)(struct IDiscordImageManager *, struct DiscordImageHandle, bool, void *, void (*)(void *, enum EDiscordResult, struct DiscordImageHandle))
     * }
     */
    public static final long fetch$offset() {
        return fetch$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * void (*fetch)(struct IDiscordImageManager *, struct DiscordImageHandle, bool, void *, void (*)(void *, enum EDiscordResult, struct DiscordImageHandle))
     * }
     */
    public static MemorySegment fetch(MemorySegment struct) {
        return struct.get(fetch$LAYOUT, fetch$OFFSET);
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * void (*fetch)(struct IDiscordImageManager *, struct DiscordImageHandle, bool, void *, void (*)(void *, enum EDiscordResult, struct DiscordImageHandle))
     * }
     */
    public static void fetch(MemorySegment struct, MemorySegment fieldValue) {
        struct.set(fetch$LAYOUT, fetch$OFFSET, fieldValue);
    }

    /**
     * {@snippet lang=c :
     * enum EDiscordResult (*get_dimensions)(struct IDiscordImageManager *, struct DiscordImageHandle, struct DiscordImageDimensions *)
     * }
     */
    public static class get_dimensions {

        get_dimensions() {
            // Should not be called directly
        }

        /**
         * The function pointer signature, expressed as a functional interface
         */
        public interface Function {
            int apply(MemorySegment _x0, MemorySegment _x1, MemorySegment _x2);
        }

        private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
            discord_game_sdk_h.C_INT,
            discord_game_sdk_h.C_POINTER,
            DiscordImageHandle.layout(),
            discord_game_sdk_h.C_POINTER
        );

        /**
         * The descriptor of this function pointer
         */
        public static FunctionDescriptor descriptor() {
            return $DESC;
        }

        private static final MethodHandle UP$MH = discord_game_sdk_h.upcallHandle(Function.class, "apply", $DESC);

        /**
         * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
         * The lifetime of the returned segment is managed by {@code arena}
         */
        public static MemorySegment allocate(Function fi, Arena arena) {
            return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
        }

        private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

        /**
         * Invoke the upcall stub {@code funcPtr}, with given parameters
         */
        public static int invoke(MemorySegment funcPtr,MemorySegment _x0, MemorySegment _x1, MemorySegment _x2) {
            try {
                return (int) DOWN$MH.invokeExact(funcPtr, _x0, _x1, _x2);
            } catch (Throwable ex$) {
                throw new AssertionError("should not reach here", ex$);
            }
        }
    }

    private static final AddressLayout get_dimensions$LAYOUT = (AddressLayout)$LAYOUT.select(groupElement("get_dimensions"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_dimensions)(struct IDiscordImageManager *, struct DiscordImageHandle, struct DiscordImageDimensions *)
     * }
     */
    public static final AddressLayout get_dimensions$layout() {
        return get_dimensions$LAYOUT;
    }

    private static final long get_dimensions$OFFSET = 8;

    /**
     * Offset for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_dimensions)(struct IDiscordImageManager *, struct DiscordImageHandle, struct DiscordImageDimensions *)
     * }
     */
    public static final long get_dimensions$offset() {
        return get_dimensions$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_dimensions)(struct IDiscordImageManager *, struct DiscordImageHandle, struct DiscordImageDimensions *)
     * }
     */
    public static MemorySegment get_dimensions(MemorySegment struct) {
        return struct.get(get_dimensions$LAYOUT, get_dimensions$OFFSET);
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_dimensions)(struct IDiscordImageManager *, struct DiscordImageHandle, struct DiscordImageDimensions *)
     * }
     */
    public static void get_dimensions(MemorySegment struct, MemorySegment fieldValue) {
        struct.set(get_dimensions$LAYOUT, get_dimensions$OFFSET, fieldValue);
    }

    /**
     * {@snippet lang=c :
     * enum EDiscordResult (*get_data)(struct IDiscordImageManager *, struct DiscordImageHandle, uint8_t *, uint32_t)
     * }
     */
    public static class get_data {

        get_data() {
            // Should not be called directly
        }

        /**
         * The function pointer signature, expressed as a functional interface
         */
        public interface Function {
            int apply(MemorySegment _x0, MemorySegment _x1, MemorySegment _x2, int _x3);
        }

        private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
            discord_game_sdk_h.C_INT,
            discord_game_sdk_h.C_POINTER,
            DiscordImageHandle.layout(),
            discord_game_sdk_h.C_POINTER,
            discord_game_sdk_h.C_INT
        );

        /**
         * The descriptor of this function pointer
         */
        public static FunctionDescriptor descriptor() {
            return $DESC;
        }

        private static final MethodHandle UP$MH = discord_game_sdk_h.upcallHandle(Function.class, "apply", $DESC);

        /**
         * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
         * The lifetime of the returned segment is managed by {@code arena}
         */
        public static MemorySegment allocate(Function fi, Arena arena) {
            return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
        }

        private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

        /**
         * Invoke the upcall stub {@code funcPtr}, with given parameters
         */
        public static int invoke(MemorySegment funcPtr,MemorySegment _x0, MemorySegment _x1, MemorySegment _x2, int _x3) {
            try {
                return (int) DOWN$MH.invokeExact(funcPtr, _x0, _x1, _x2, _x3);
            } catch (Throwable ex$) {
                throw new AssertionError("should not reach here", ex$);
            }
        }
    }

    private static final AddressLayout get_data$LAYOUT = (AddressLayout)$LAYOUT.select(groupElement("get_data"));

    /**
     * Layout for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_data)(struct IDiscordImageManager *, struct DiscordImageHandle, uint8_t *, uint32_t)
     * }
     */
    public static final AddressLayout get_data$layout() {
        return get_data$LAYOUT;
    }

    private static final long get_data$OFFSET = 16;

    /**
     * Offset for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_data)(struct IDiscordImageManager *, struct DiscordImageHandle, uint8_t *, uint32_t)
     * }
     */
    public static final long get_data$offset() {
        return get_data$OFFSET;
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_data)(struct IDiscordImageManager *, struct DiscordImageHandle, uint8_t *, uint32_t)
     * }
     */
    public static MemorySegment get_data(MemorySegment struct) {
        return struct.get(get_data$LAYOUT, get_data$OFFSET);
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * enum EDiscordResult (*get_data)(struct IDiscordImageManager *, struct DiscordImageHandle, uint8_t *, uint32_t)
     * }
     */
    public static void get_data(MemorySegment struct, MemorySegment fieldValue) {
        struct.set(get_data$LAYOUT, get_data$OFFSET, fieldValue);
    }

    /**
     * Obtains a slice of {@code arrayParam} which selects the array element at {@code index}.
     * The returned segment has address {@code arrayParam.address() + index * layout().byteSize()}
     */
    public static MemorySegment asSlice(MemorySegment array, long index) {
        return array.asSlice(layout().byteSize() * index);
    }

    /**
     * The size (in bytes) of this struct
     */
    public static long sizeof() { return layout().byteSize(); }

    /**
     * Allocate a segment of size {@code layout().byteSize()} using {@code allocator}
     */
    public static MemorySegment allocate(SegmentAllocator allocator) {
        return allocator.allocate(layout());
    }

    /**
     * Allocate an array of size {@code elementCount} using {@code allocator}.
     * The returned segment has size {@code elementCount * layout().byteSize()}.
     */
    public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
        return allocator.allocate(MemoryLayout.sequenceLayout(elementCount, layout()));
    }

    /**
     * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
     * The returned segment has size {@code layout().byteSize()}
     */
    public static MemorySegment reinterpret(MemorySegment addr, Arena arena, Consumer<MemorySegment> cleanup) {
        return reinterpret(addr, 1, arena, cleanup);
    }

    /**
     * Reinterprets {@code addr} using target {@code arena} and {@code cleanupAction} (if any).
     * The returned segment has size {@code elementCount * layout().byteSize()}
     */
    public static MemorySegment reinterpret(MemorySegment addr, long elementCount, Arena arena, Consumer<MemorySegment> cleanup) {
        return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup);
    }
}

